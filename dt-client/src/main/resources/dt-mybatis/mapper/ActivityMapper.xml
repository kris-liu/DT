<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.blogxin.pay.mapper.PayOrderMapper">

    <resultMap id="baseResultMap" type="cn.blogxin.dt.client.log.repository.mybatis.ActivityMapper">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="xid" property="xid" jdbcType="VARCHAR"/>
        <result column="name" property="name" jdbcType="VARCHAR"/>
        <result column="status" property="status" jdbcType="INTEGER"/>
    </resultMap>

    <sql id="baseColumnList">
        id, xid, name, status
    </sql>

    <insert id="insert">
        INSERT INTO
          activity
          (xid, name, status)
        VALUES
          (#{xid}, #{name})
    </insert>

    <select id="query" parameterType="java.lang.String" resultMap="baseResultMap">
        SELECT
          <include refid="baseColumnList"/>
        FROM
          activity
        WHERE
          xid = #{xid}
    </select>


</mapper>